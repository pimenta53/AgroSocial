<%= form_for(@feedsnet) do |f| %>
  <% if @feedsnet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedsnet.errors.count, "error") %> prohibited this feedsnet from being saved:</h2>

      <ul>
      <% @feedsnet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
